{
  "title": "C4OS Grill Question 003 - Tool View Selection",
  "source_skill": "grill-with-docs",
  "source_workflow": "combined-spec-grill",
  "questions": [
    {
      "id": "tool_view_selection",
      "label": "When multiple app plugins support the same Tauri tool, how should C4OS choose the view?",
      "type": "radio",
      "required": true,
      "help": "Recommended: let the user choose a default view per tool, with plugin priority only as the initial fallback. This supports replacing the default Browser plugin without coupling tool authority to one app plugin.",
      "options": [
        "User chooses default view per tool; plugin priority is fallback",
        "Highest-priority enabled plugin always owns the tool view",
        "Runtime chooses the view from available plugin metadata",
        "Show all compatible views every time",
        "Other"
      ]
    },
    {
      "id": "tool_view_selection_notes",
      "label": "Notes or corrections",
      "type": "textarea",
      "required": false,
      "help": "Clarify whether this setting is global, workspace-scoped, session-scoped, or controlled by each plugin's config."
    }
  ]
}
