{
  "title": "C4OS Grill Question 013 - Center Pane And Panel Resize",
  "source_skill": "grill-with-docs",
  "source_workflow": "combined-spec-grill",
  "questions": [
    {
      "id": "center_pane_minimum",
      "label": "What should the center chat pane minimum width be?",
      "type": "radio",
      "required": true,
      "help": "Recommended: 560px minimum. It is wide enough for readable chat composition and responses while leaving room for one or two side panels on common desktop widths.",
      "options": [
        "560px",
        "640px",
        "720px",
        "Use responsive breakpoint rules instead of one fixed value",
        "Other"
      ]
    },
    {
      "id": "panel_resize_collision",
      "label": "What happens when side panels would shrink the center below its minimum?",
      "type": "radio",
      "required": true,
      "help": "Recommended: stop resizing at the center minimum and prevent panels from expanding further. This keeps the chat pane usable and predictable.",
      "options": [
        "Stop panel expansion at the center minimum",
        "Allow center to shrink and rely on responsive layout",
        "Automatically close the opposite-side panel",
        "Automatically collapse the wider panel",
        "Other"
      ]
    },
    {
      "id": "center_pane_resize_notes",
      "label": "Notes or corrections",
      "type": "textarea",
      "required": false,
      "help": "Clarify whether panel widths persist per chat session, per plugin, or globally, and whether narrow windows should force panels closed."
    }
  ]
}
