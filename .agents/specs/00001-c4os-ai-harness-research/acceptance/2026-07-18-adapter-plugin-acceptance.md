# Adapter and Plugin Decision Acceptance — 2026-07-18

The user accepted these research decisions:

- `P-002 — C4OS owns policy above both adapters`: accepted.
- `P-003 — OpenCode runs as a managed server`: accepted.
- `P-004 — Pi runs through a C4OS Node SDK sidecar`: accepted.
- `P-005 — Plugins begin as declarative bundles`: accepted.

The user subsequently accepted:

- `P-001 — Treat r012 as current product intent`.
- `P-006 — Allow arbitrary websites while isolating them from C4OS`, provided C4OS permission mediation preserves expected browser behavior and does not blanket-block browser capabilities when a C4OS-specific prompt is unavailable.
- `P-008 — Exclude Codex import from current product scope`; Codex is standards research, not an implied import target.
- `P-009 — Treat marketplaces as plugin catalogs`; adding a catalog lists plugins, while the selected plugin's installation lifecycle remains C4OS-owned.
