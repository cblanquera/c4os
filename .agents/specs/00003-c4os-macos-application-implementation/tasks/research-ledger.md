# Bounded Online Research And Blocker Ledger

Scope: implementation dependency/version decisions and concrete blockers only. Access dates for current entries run through 2026-07-22.

## RBL-001 — Initial renderer and Tauri lock

- **Decision investigated:** Current compatible patch versions before the greenfield production scaffold is locked.
- **Queries and primary sources:** Tauri releases; npm registry pages for React, Vite, React Router, Redux Toolkit, and React Aria Components.
- **Exact versions observed:** Tauri core `2.11.5`; Tauri CLI `2.11.4`; React `19.2.7`; Vite `8.1.5`; React Router `8.2.0`; Redux Toolkit `2.12.0`; React Aria Components `1.19.0`.
- **Selected conclusion:** Preserve the Frozen major/minor architecture and begin Task 00001 with these current stable patch baselines. Lock the resolved compatible Tauri package set and auxiliary test/tool versions in committed lockfiles after build/test verification.
- **Rejected alternatives:** React canary/experimental builds, Vite 9 scaffolding, server-rendered frameworks, visual component kits, and unverified Tauri version mixing.
- **Implementation impact:** Task 00001 owns the lockfiles, engine constraints, typed boundary, and smoke matrix.
- **Residual risk:** Tauri crates and CLI publish independently; the resolved set must be verified by `cargo check`, native launch, and lockfile inspection before the task passes.
- **Sources:** https://github.com/tauri-apps/tauri/releases ; https://www.npmjs.com/package/react ; https://www.npmjs.com/package/vite ; https://www.npmjs.com/package/react-router ; https://www.npmjs.com/package/%40reduxjs/toolkit ; https://www.npmjs.com/package/react-aria-components

### Locked auxiliary row

- **Runtime dependencies:** `@tauri-apps/api 2.11.1`, React DOM `19.2.7`, React Redux `9.3.0`.
- **Build and QA dependencies:** TypeScript `6.0.2`, `@vitejs/plugin-react 6.0.3`, Vitest `4.1.10`, Testing Library React `16.3.2`, Playwright Test `1.61.1`, ESLint `10.7.0`, `typescript-eslint 8.64.0`, Prettier `3.9.5`, jsdom `29.1.1`.
- **Rust protocol dependencies:** `ts-rs 12.0.1`, Serde `1.0.228`, serde_json `1.0.150`, thiserror `2.0.18`, uuid `1.24.0`, tracing `0.1.44`, tauri-build `2.6.3`.
- **Compatibility decision:** The registry's TypeScript `7.0.2` was rejected for this lock because `typescript-eslint 8.64.0` declares TypeScript `<6.1.0`; `6.0.2` is the newest compatible stable line. The selected package graph requires Node `>=22.19.0` because that is the higher renderer/Pi floor.
- **Verification:** Exact direct trees, clean installs, compilation, native bundling/launch, type generation, npm audit, and RustSec audit all completed before Task 00001 passed.

## RBL-002 — Rust persistence, archive, and WebKit baselines

- **Decision investigated:** Whether Frozen Rust baselines remain current and mutually compatible.
- **Exact versions observed:** `rusqlite 0.40.1` with bundled SQLite `3.53.2`; `rusqlite_migration 2.6.0`; `zip 8.6.0`; `objc2-web-kit 0.3.2`.
- **Selected conclusion:** Keep the exact Frozen selections. Enable only required `zip` features, including Deflate, rather than its broad default compression/crypto set.
- **Rejected alternatives:** `zip 9.0.0-pre*`, system SQLite, an ORM/async ownership layer, Wry for arbitrary websites, and private WebKit APIs.
- **Implementation impact:** Tasks 00001, 00002, and 00010 lock and verify these versions.
- **Residual risk:** Target API availability and macOS binding interactions remain production integration work despite the passing Proof.
- **Sources:** https://docs.rs/crate/rusqlite/latest ; https://docs.rs/crate/rusqlite_migration/latest ; https://docs.rs/crate/zip/latest ; https://docs.rs/objc2-web-kit/latest/objc2_web_kit/

### Task 00002 auxiliary lock

- **Exact versions observed from crates.io metadata:** TOML `1.1.3+spec-1.1.0`, SHA-2 `0.11.0`, fs4 `1.1.0`, notify `8.2.0`, tempfile `3.27.0` for tests.
- **Selected conclusion:** Lock TOML's stable spec-1.1 parser, the current RustCrypto SHA-2 line, maintained pure-Rust fs4 synchronous advisory locking, stable notify 8.2 rather than the 9.0 release candidate, and test-only tempfile. Lock `zip 8.6.0` with only `deflate-flate2-zlib-rs`; do not enable its default crypto and alternate-compression graph.
- **Rejected alternatives:** Deprecated fs2 `0.4.3`, notify `9.0.0-rc.4`, SHA-2 `0.10.9` when the compatible stable `0.11.0` exists, broad zip defaults, and handwritten TOML or archive parsing.
- **Verification gate:** Cargo resolution, target tree, RustSec audit, hostile archive/configuration tests, and production integration remain required before Task 00002 passes.
- **Sources:** https://crates.io/crates/toml/1.1.3+spec-1.1.0 ; https://crates.io/crates/sha2/0.11.0 ; https://crates.io/crates/fs4/1.1.0 ; https://crates.io/crates/notify/8.2.0 ; https://crates.io/crates/tempfile/3.27.0

## RBL-003 — OpenCode adapter baseline

- **Decision investigated:** Current native runtime and SDK pair for authenticated loopback integration.
- **Exact versions observed:** OpenCode release and `@opencode-ai/sdk` both `1.18.3` on 2026-07-18.
- **Selected conclusion:** Pin both to `1.18.3` for the first adapter compatibility row; start `opencode serve` on loopback with C4OS-owned Basic authentication, isolated state, health/version checks, and the official typed SDK/OpenAPI surface.
- **Rejected alternatives:** The archived 2025 Go project with the same name, unauthenticated server use, renderer-direct access, and floating `latest` at runtime.
- **Implementation impact:** Task 00004 records the compatibility row and conformance evidence.
- **Residual risk:** OpenCode releases rapidly; C4OS must retain a compatibility matrix and last-known-good runtime rather than auto-adopting updates.
- **Sources:** https://github.com/anomalyco/opencode/releases ; https://www.npmjs.com/package/%40opencode-ai/sdk ; https://opencode.ai/docs/server/ ; https://opencode.ai/docs/sdk/

## RBL-004 — Pi SDK ownership transfer

- **Decision investigated:** The selected Pi SDK package changed maintainers/scope after Freeze.
- **Exact versions observed:** Deprecated `@mariozechner/pi-coding-agent 0.73.1`; maintained `@earendil-works/pi-coding-agent 0.80.10`, `@earendil-works/pi-agent-core 0.80.10`, and `@earendil-works/pi-ai 0.80.10` in the implemented lock graph.
- **Selected conclusion:** Use the official maintained successor `@earendil-works/pi-coding-agent 0.80.10` through the documented programmatic SDK inside the C4OS-owned Node sidecar. This is a bounded dependency substitution, not a product or authority change.
- **Rejected alternatives:** Pinning the deprecated Mario-scope package, granting Pi's own extensions ambient authority, using Pi persistence as C4OS authority, or replacing the selected SDK sidecar with its CLI/RPC without need.
- **Implementation impact:** Task 00004 must define a narrow wrapper, disable/replace native tools with C4OS action requests, and conformance-test event barriers, cancellation, capabilities, and redaction.
- **Residual risk:** The successor publishes frequently; lock the full dependency graph and re-run the exact coding-agent/core/AI compatibility suite before changing any package version.
- **Sources:** https://www.npmjs.com/package/%40mariozechner/pi-coding-agent ; https://www.npmjs.com/package/%40earendil-works/pi-coding-agent ; https://www.npmjs.com/package/%40earendil-works/pi-agent-core ; https://github.com/earendil-works/pi/blob/main/packages/coding-agent/docs/sdk.md

## RBL-005 — Local toolchain baseline

- **Exact environment:** Node `26.2.0`, npm `11.13.0`, Rust/Cargo `1.96.0`, macOS `26.5.1 (25F80)` arm64, Xcode `26.6 (17F113)`.
- **Selected conclusion:** Record Node `>=22.19` as the supported development floor required by the selected Pi SDK and renderer tooling while verifying the current Node 26 host; use Rust 2024 edition with a pinned toolchain file after the first successful scaffold build.
- **Rejected alternatives:** Depending on the user's shell-specific version manager or treating the current host version as the only supported source contract.
- **Implementation impact:** Task 00001 adds engine/toolchain declarations and repeatable commands.
- **Residual risk:** Native dependencies may expose Node 26 or Xcode 26 regressions; research exact upstream issues before workarounds or downgrades.

## RBL-006 — Existing evidence and production baseline

- **Decision investigated:** Whether any current source or Proof can count as production implementation or production Agent Acceptance.
- **Observed baseline:** No root application manifest, production source tree, production test tree, or production lockfile exists. The accepted r013 wireframe is a static in-memory fixture; all 50 Feature Coverage IDs are open.
- **Reusable evidence boundary:** Existing Proofs may supply feasibility constraints and negative test scenarios only. Runtime adapter conformance passes `11/11`; the broad root Proof command currently reports `77/85` passing, with eight environment-dependent Docker, loopback-listener, and nested-sandbox failures.
- **Selected conclusion:** Build greenfield production modules behind shared coordinator-owned schemas. Do not incrementally convert the r013 monolith or import Proof success as task evidence. Split future tests into deterministic, native macOS, Docker/SSH, provider/network, and GUI tiers.
- **Rejected alternatives:** Claiming source-only readiness, using r013 timers/localStorage/DOM snapshots as production logic, and retaining stale Proof assertions that share Chats across Workspaces, delete removed Chats, terminate PTYs on Chat inactivation, keep a right panel, or use raw Wry for arbitrary pages.
- **Implementation impact:** Every task starts `open` with Agent Acceptance `failed`; each closes only from production evidence. Tasks 00001 and 00015 own tiered commands and a deterministic QA adapter.
- **Residual risk:** Environment-specific suites require explicit host capabilities and must remain visible rather than being silently excluded from completion evidence.

## RBL-007 — Locked-graph advisory disposition

- **Decision investigated:** Whether the first exact npm and Cargo graphs contain known vulnerabilities or unacceptable warnings.
- **Observed result:** Full `npm audit` reports zero vulnerabilities. After the Task 00002 persistence/archive dependencies were locked, `cargo audit` scans 460 locked Rust dependencies and reports zero vulnerabilities plus 17 warnings: 16 unmaintained advisories and one unsound glib advisory.
- **Target analysis:** `cargo tree --target aarch64-apple-darwin -i glib` returns no path; GTK/glib warnings are target-specific and absent from the accepted arm64 macOS build. The unmaintained `unic-*` chain is reachable through Tauri `2.11.5` to tauri-utils `2.9.3` to urlpattern `0.3.0` and has no RustSec vulnerability finding.
- **Selected conclusion:** Accept the exact local macOS foundation graph with the warnings recorded, retain current Tauri patches, and re-audit on dependency updates. Do not force unsupported transitive replacements.
- **Rejected alternatives:** Ignoring advisory output, claiming warning-free Rust dependencies, patching registry crates locally without an upstream compatibility reason, or expanding the milestone to Linux GTK support.
- **Implementation impact:** Task 00001 passes; RBL-007 remains a visible maintenance input and does not close later security acceptance.
- **Residual risk:** Upstream maintenance status can change. Any new vulnerability, reachable unsoundness on the accepted target, or Tauri patch row must reopen the owning dependency task.

## RBL-008 — Credential encryption and macOS installation-key custody

- **Access date:** 2026-07-19.
- **Decision investigated:** The smallest maintained Rust dependency set that implements the Frozen credential contract: macOS Keychain custody for the installation key, authenticated encryption for durable credential values, memory-hard password verification, random nonces/salts/tokens, and zeroization of owned secret buffers.
- **Exact versions observed:** `security-framework 3.7.0`, `chacha20poly1305 0.11.0`, `argon2 0.5.3`, `getrandom 0.3.4`, and `zeroize 1.9.0`.
- **Selected conclusion:** Use the platform Security framework only for the random installation key; encrypt vault records with XChaCha20-Poly1305 and record-bound associated data; use Argon2id PHC strings for password credentials; use the operating-system RNG for keys, nonces, salts, and opaque delivery tokens; zeroize owned plaintext and key buffers. Persist only opaque credential references and authenticated ciphertext metadata outside Keychain. Keep session-only credentials exclusively in memory and deliver decrypted values only through short-lived, single-consumer leases.
- **Rejected alternatives:** Persisting plaintext, using a fast digest as a password hash, placing the entire credential corpus in Keychain, browser storage, unauthenticated encryption, a new immature cross-platform security wrapper, and stale keyring abstractions that would dilute the exact macOS custody contract.
- **Implementation impact:** Task 00003 owns the credential vault, Keychain adapter, reauthentication/import state, lease invalidation, redaction/scanning, hostile corruption tests, and restart evidence. Task 00013 may expose only opaque references and safe metadata through Settings.
- **Residual risk:** CI and non-macOS unit tests cannot prove a live login Keychain interaction. The macOS acceptance tier must exercise the platform adapter in the unlocked user session without printing or persisting secret material; any Keychain access-control prompt or OS behavior change remains visible native evidence rather than being substituted with the deterministic test backend.
- **Sources:** https://github.com/kornelski/rust-security-framework ; https://github.com/RustCrypto/AEADs/tree/master/chacha20poly1305 ; https://github.com/RustCrypto/password-hashes/tree/master/argon2 ; https://docs.rs/getrandom/0.3.4/getrandom/ ; https://docs.rs/zeroize/1.9.0/zeroize/

## RBL-009 — OpenCode native authority boundary

- **Access date:** 2026-07-19.
- **Decision investigated:** Whether OpenCode's native permission replies can be treated as C4OS authorization and whether a C4OS approval may be returned as native `once`.
- **Observed upstream contract:** OpenCode `1.18.3` exposes an OpenAPI 3.1 server and generated SDK; its server uses `OPENCODE_SERVER_PASSWORD` for HTTP Basic authentication. Its permission configuration defaults most tools to `allow`, accepts global and tool-specific `allow`/`ask`/`deny`, and its native approval outcomes include `once`, session-scoped `always`, and `reject`.
- **Selected conclusion:** OpenCode permission state is not C4OS policy authority. Launch the exact native process on authenticated loopback with default `deny`, allowing only the exact C4OS-owned `c4os_propose_action` and `c4os_read_resource` broker tools. Per request, expose only those broker IDs. Route proposal identity through the sealed C4OS Action Gateway, execute effects only in a C4OS worker, and return native `reject` even after a C4OS effect completes so OpenCode itself never receives effect authority. This is a C4OS defense-in-depth inference from the upstream permission and server contracts, not an upstream claim that OpenCode supplies an OS sandbox.
- **Rejected alternatives:** Wildcard `ask`, native `once` after C4OS approval, native `always`, built-in write/bash execution, renderer-originated permission decisions, relying on permissive defaults, and describing OpenCode permissions as the C4OS security boundary.
- **Implementation impact:** Task 00004 owns digest-pinned authority configuration, isolated state, authenticated transport, broker-only per-request tools, exact intent binding, fail-closed native replies, and negative tests proving direct native-effect paths are unavailable.
- **Residual risk:** The two broker tool definitions must remain C4OS-owned and digest verified when materialized for the native runtime. Any upstream permission, custom-tool, or server API change must create a new compatibility row rather than silently widening authority.
- **Sources:** https://opencode.ai/docs/permissions/ ; https://opencode.ai/docs/server/ ; https://opencode.ai/docs/sdk/ ; local exact `@opencode-ai/sdk 1.18.3` generated OpenAPI types.

## RBL-010 — OpenCode provider credential delivery

- **Access date:** 2026-07-19.
- **Decision investigated:** How an exact OpenCode `1.18.3` worker can authenticate a selected C4OS provider without moving the raw provider credential into ordinary runtime state, command arguments, inherited environment, logs, renderer data, or native-owned authority.
- **Queries and primary sources:** OpenCode provider/auth documentation and source; exact local `@opencode-ai/sdk 1.18.3` generated `AuthSetData` types; exact local `@opencode-ai/plugin 1.18.3` `chat.headers` hook types; upstream credential-storage issue and current plugin examples.
- **Observed upstream contract:** The generated SDK's standard `PUT /auth/{id}` path accepts the raw provider auth value. OpenCode documents that provider credentials are stored in `auth.json`, and its current CLI/source paths also recognize environment/config credentials. The exact plugin contract can inject request headers, but it supplies no C4OS vault or operation-lease boundary by itself.
- **Selected conclusion:** Do not call the native auth persistence API and do not place provider keys in OpenCode configuration or environment. Extend the already authenticated, descriptor-bound C4OS plugin bridge with a separate bounded credential descriptor. Rust leases the exact provider credential for the active session operation, delivers one correlation/provider/session-bound frame, and the C4OS plugin consumes it once while constructing that request's provider header. The secret channel is distinct from the Action Gateway broker so ordinary broker frames remain secret-rejecting.
- **Rejected alternatives:** Native `auth.json`, `PUT /auth/{id}`, provider keys in `opencode.json`, `.env`, or inherited environment, renderer delivery, command-line delivery, a long-lived plaintext sidecar cache, weakening the broker's secret scanner, and declaring OpenCode provider authentication unsupported while presenting the runtime as ready.
- **Implementation impact:** Task 00004 must wire the private descriptor through the exact OpenCode launcher/plugin graph, derive the provider credential reference inside Rust-owned route state, deliver it before the exact dispatch, consume it once in `chat.headers`, and prove with live native tests and state/log/argv/environment scans that the raw value is absent outside the channel and transient worker memory.
- **Residual risk:** OpenCode's plugin/auth internals can change rapidly. Any upgrade must re-run the exact live credentialed dispatch and secret-absence matrix; the standard upstream auth API remains intentionally unused.
- **Sources:** https://opencode.ai/docs/providers/ ; https://opencode.ai/docs/cli/ ; https://github.com/anomalyco/opencode/issues/5423 ; https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/plugin/codex.ts ; local exact `@opencode-ai/sdk 1.18.3` and `@opencode-ai/plugin 1.18.3` type surfaces.

## RBL-011 — OpenCode server-secret descriptor build

- **Access date:** 2026-07-19.
- **Decision investigated:** Whether the stock OpenCode `1.18.3` executable can satisfy the Frozen requirement that the authenticated loopback server secret never enter arguments, the inherited environment, logs, configuration, or persistent runtime state.
- **Observed upstream contract:** Exact tag `v1.18.3` at commit `127bdb30784d508cc556c71a0f32b508a3061517` reads the server password from `OPENCODE_SERVER_PASSWORD`. Stock bytes therefore cannot meet the stricter C4OS secret-absence contract even though the generated SDK/OpenAPI and native compatibility version remain usable.
- **Selected conclusion:** Build a distinct `1.18.3+c4os-auth-fd.1` artifact from that exact tag. The bounded patch reads the server secret once from the inherited `C4OS_OPENCODE_SERVER_SECRET_FD`, rejects simultaneous environment password delivery, clears descriptor metadata, closes the descriptor, and preserves stock behavior only outside the C4OS launch. Pin Node `26.3.0` (`sha256:cdf556966c52b321abb07cd565edb5fb2ca61c467f84a434bb28e0d33a3c9580`), Bun `1.3.14` (`sha256:e0c90ec15d33363e6b70713d56bc3b2c7585c17f40a0fe0f8fd9305901d4e233`), patch `sha256:82af7d9b917e6fe31fa524b37840cdd95f86944e9ea0c566eb0e061020bc99c0`, the compressed models.dev snapshot `sha256:bc9565e9e805f3a5496e674dccbce5ed0338dabf0e2d93c6ddbf41d00533216d`, and the resulting arm64 binary `sha256:e117af53becb62e91a8b8ba4e4965aeab3ec77082e31c118420f2b779753bc77` (137,518,946 bytes). Two independent clean dependency reconstructions produced those exact bytes. Runtime provenance records the C4OS flavor separately and never calls it the official npm binary.
- **Rejected alternatives:** Shipping the raw password in `OPENCODE_SERVER_PASSWORD`, placing it in argv/configuration, weakening the Frozen secret contract, silently substituting unrecorded bytes, or claiming the downstream artifact is the official npm release.
- **Implementation impact:** `sidecars/opencode-native/c4os-build.json` is the build-input ledger; `build-c4os.mjs --force` is restricted to the build-owned checkout, rejects unexpected untracked source, clears ignored dependency/build state, restores the frozen lockfile under an allowlisted environment with no inherited Sentry/Vite credentials, and compares the rebuilt digest; the production asset factory re-verifies the manifest, patch, model snapshot, complete native tree, executable digest, version, and build flavor before spawn. Exact native tests must scan both server and provider secrets across process and state surfaces.
- **Residual risk:** OpenCode may change its auth initialization or build inputs in a future release. Every upgrade requires a new compatibility row, patch review, forced reproducibility build, exact-native golden path, and renewed secret scan. The server secret necessarily exists in transient OpenCode worker memory while Basic authentication is active; no other surface is permitted.
- **Sources:** https://github.com/anomalyco/opencode/blob/v1.18.3/packages/opencode/src/server/auth.ts ; https://github.com/anomalyco/opencode/blob/v1.18.3/packages/opencode/script/build.ts ; https://opencode.ai/docs/server/ ; pinned local downstream patch and build manifest.

## RBL-012 — Published C4OS OpenCode proof reuse boundary

- **Access date:** 2026-07-20.
- **Decision investigated:** Whether the OpenCode source already published in `cblanquera/c4os` materially helps Task 00004 and can replace any production evidence.
- **Observed repository state:** Published `main` contains `proofs/opencode-runtime/opencode_runtime_poc.mjs` and its evidence note. The proof used OpenCode `1.17.8` and demonstrated isolated SDK start, session create/resume, typed event observation, abort, and shutdown. Its evidence explicitly did not prove a credentialed model prompt, live approval path, or application-owned persistent state. The local `build/mvp-3` Task 00004 source is not published on the repository's current branch list.
- **Selected conclusion:** Reuse the published proof only as architectural orientation for the SDK/session/event/abort sequence. The production `1.18.3` composition, private credential paths, C4OS Action Gateway, exact attachment delivery, persistence, capability atomicity, and cleanup must remain locally verified Task 00004 evidence.
- **Rejected alternatives:** Treating the older proof as production acceptance, downgrading to its `1.17.8` pin, or publishing the local implementation without an explicit user request.
- **Implementation impact:** No source was copied and no push occurred. The live repository check confirmed that Task 00004's local production path materially supersedes the published proof.
- **Residual risk:** Published proof wording can become misleading if read without its evidence boundary; future documentation should link the production compatibility row when the implementation is intentionally published.
- **Sources:** https://github.com/cblanquera/c4os ; https://github.com/cblanquera/c4os/blob/main/proofs/opencode-runtime/opencode_runtime_poc.mjs ; https://github.com/cblanquera/c4os/blob/main/proofs/opencode-runtime/opencode-runtime-evidence-2026-06-20.md

## RBL-013 — Private native TLS verification capability

- **Access date:** 2026-07-20.
- **Decision investigated:** How to prove the exact authenticated OpenCode/Pi provider paths without persistently altering the user's login or System keychain.
- **Observed blocker:** macOS rejected the serialized System.keychain trust attempts before a usable administrator interaction completed. Each attempt was followed by exact certificate, keychain search-list, lock, and temporary-file reconciliation; no trust item remained.
- **Selected conclusion:** Generate an ephemeral private CA and leaf inside a mode-`0700` temporary root, pass the CA only to the ignored native test through a test-only capability, require machine trust to reject the leaf before and after the run, and clean the isolated Cargo/native process group plus temporary material under a wrapper trap. Production provider TLS continues to use normal platform trust.
- **Rejected alternatives:** Leaving a fingerprint-bound test root in System.keychain, weakening TLS verification, disabling certificate validation, or claiming browser/provider success from an untrusted local endpoint.
- **Implementation impact:** `scripts/run-task-00004-native-golden.zsh` owns the private capability, process isolation, trust pre/postcondition, evidence path, and cleanup invariant. The final post-bundle run passed.
- **Residual risk:** This proves the app-owned TLS/authentication path under a private test capability, not enterprise proxy/root deployment behavior. Platform trust-store integration remains a deployment concern rather than a Task 00004 runtime authority.
- **Sources:** project-owned native wrapper, production integration test, and final evidence JSON.

## RBL-014 — macOS menu, theme, and native picker composition

- **Access date:** 2026-07-20.
- **Decision investigated:** How Task 00005 should compose a system-following initial theme, the native application Settings menu and `Cmd+,`, and Rust-owned file/folder/workspace pickers on the locked Tauri line.
- **Observed upstream contract:** Tauri `2.11.5` exposes the current window theme, app-wide menus and menu events, standard menu builders, and accelerator parsing where `CmdOrCtrl` maps to Command on macOS. The official `tauri-plugin-dialog 2.7.2` supports Rust-side non-blocking pickers and blocking pickers from asynchronous command contexts; its blocking API must not run on the main thread. Apple documents that a `nil` app appearance follows the current system appearance and that effective appearances and adaptive colors are the native Dark Mode model.
- **Selected conclusion:** Keep the standard decorated window and system-owned appearance, capture Tauri's current native window theme before reveal, retain an independent webview `prefers-color-scheme` listener for live changes, build an app-wide native menu with a typed Settings event and `CmdOrCtrl+,`, and pin `tauri-plugin-dialog 2.7.2` for Rust-owned opaque-grant pickers. Do not grant the renderer the dialog plugin's generic command surface or raw filesystem authority.
- **Rejected alternatives:** A custom titlebar, a persisted C4OS theme override, dependence on `WindowEvent::ThemeChanged`, renderer-owned generic dialog calls, blocking a picker on the main thread, and exposing selected paths as ambient renderer filesystem authority.
- **Implementation impact:** Task 00005 owns the PlatformService snapshot, pre-reveal/reveal handshake, typed Settings event, native menu, picker-grant lifecycle, semantic theme listener, and exact native/rendered tests. Task 00006 and Task 00013 retain the full shell and Settings content.
- **Residual risk:** The rebuilt macOS app passed the Task 00005 initial/live theme, shortcut, cancellation, grant-isolation, focus, responsive, and picker matrix; Task 00015B must repeat the integrated native/accessibility audit. Any Tauri or dialog-plugin upgrade requires repeating the matrix.
- **Sources:** https://docs.rs/tauri/2.11.5/tauri/struct.App.html ; https://docs.rs/tauri/2.11.5/tauri/menu/ ; https://docs.rs/tauri/2.11.5/tauri/window/struct.Window.html ; https://docs.rs/tauri-plugin-dialog/2.7.2/tauri_plugin_dialog/struct.FileDialogBuilder.html ; https://developer.apple.com/documentation/appkit/nsapplication/appearance ; https://developer.apple.com/documentation/appkit/nsappearance

## RBL-015 — Task 00009 portable PTY and terminal renderer lock

- **Access date:** 2026-07-21.
- **Decision investigated:** Which maintained Rust PTY and xterm.js packages satisfy the Task 00009 macOS terminal, resize, Stop, reconnect, and bounded-streaming contract.
- **Observed baseline:** The Rust workspace uses Rust 1.96 and edition 2024 with exact direct pins and already locks compatible `anyhow`, `libc`, and `log`; the React/Vite package uses exact pins, Node `>=22.19`, TypeScript 6, and package-lock v3. No PTY or xterm package was present.
- **Selected conclusion:** Lock `portable-pty = "=0.9.0"`, `@xterm/xterm = "6.0.0"`, and `@xterm/addon-fit = "0.11.0"`. Rust remains the canonical owner of bounded bytes, sequencing, replay, and exit state. Blocking PTY reads run on dedicated threads behind a bounded channel. Authorized Stop targets the live foreground process group through the controlling PTY and normalizes SIGINT to Interrupted with exit 130.
- **Rejected alternatives:** `ChildKiller::kill` for ordinary Stop, direct libc PTY construction, `node-pty`, legacy unscoped xterm, beta packages, addon-attach, experimental addon-serialize, WebGL, headless, clipboard, and web-link addons.
- **Implementation impact:** Task 00009 must resolve and inspect the exact lock graph, define typed sequenced byte/ack/resize/stdin/stop/exit contracts, retain the PTY master for live foreground process-group lookup, and verify real macOS SIGINT/130, resize, bounded output, reconnect, and restart behavior. Renderer state is never the reconnect authority.
- **Residual risk:** `portable-pty` uses unsafe post-fork setup and its built-in killer signals one child rather than the live foreground group; xterm writes are asynchronous and terminal output is untrusted. Native repeated-spawn, advisory, PID/process-group race, output-pressure, renderer security, and accessibility tests remain mandatory.
- **Sources:** https://crates.io/crates/portable-pty ; https://docs.rs/portable-pty/0.9.0/portable_pty/trait.MasterPty.html ; https://docs.rs/portable-pty/0.9.0/src/portable_pty/unix.rs.html ; https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man2/kill.2.html ; https://pubs.opengroup.org/onlinepubs/7908799/xbd/termios.html ; https://www.gnu.org/s/bash/manual/html_node/Exit-Status.html ; https://github.com/xtermjs/xterm.js/releases/tag/6.0.0 ; https://xtermjs.org/docs/guides/flowcontrol/ ; https://xtermjs.org/docs/guides/security/

## RBL-016 — Task 00010 production native-WebKit lock and ownership boundary

- **Access date:** 2026-07-22.
- **Decision investigated:** Whether the Frozen public-WebKit Browser boundary remains compatible with the exact production Tauri graph and which layer must own native website surfaces, profiles, request mediation, sanitized state, and teardown.
- **Exact versions observed:** Tauri `2.11.5`, tauri-build `2.6.3`, and objc2-web-kit `0.3.2` on macOS 26.5.2 arm64. The isolated Proof passed all eight boundary checks twice consecutively on that exact lock.
- **Selected conclusion:** Rust owns one public-API `WKWebView` child, its stable native controller identity, data-store/profile selection, navigation/request delegates, generation/event sequence, geometry, teardown, and bounded sanitized events. The page receives no app IPC or custom scheme. The renderer owns only accessible C4OS shell controls and a revisioned viewport measurement. Durable display URLs strip query/fragment while exact transient targets retain SHA-256 identity. `App-wide`, `Workspace/Project`, `Chat`, and `None` resolve through persisted App < Workspace < Project < Chat precedence; only the first three have persistent profile registry entries. Reply extraction uses an isolated WebKit content world and excludes storage, forms, credentials, query strings, and fragments.
- **Rejected alternatives:** Raw Wry, Tauri `WebviewWindow` website surfaces, page-accessible Tauri/Wry/C4OS/script-message bridges, renderer-owned network or profile authority, custom WebKit profile paths, renderer storage, ambient downloads, unrestricted popups/methods/binary responses, durable secret-bearing URLs, and treating the feasibility Proof as production acceptance.
- **Implementation impact:** Task 00010 owns the exact lock revalidation, Browser artifact/controller/profile/request domains, configuration-precedence resolver, native child lifecycle, public protocol, shared shell composition, hostile loopback fixture, focused/full tests, final bundle, secret scans, and native evidence. Task 00013 owns the complete Settings editing UI; Tasks 00015A and 00015B must repeat integrated security and accessibility/native audits.
- **Residual risk:** WebKit delegate and AppKit view behavior can change with macOS or objc2-web-kit upgrades; upstream Tauri patches can alter child-view geometry. Any upgrade requires a new compatibility row, two exact Proof runs, production controller/profile regressions, and the native hostile-page matrix. The page accessibility tree remains WebKit-owned and must be inspected again during Task 00015B.
- **Sources:** https://developer.apple.com/documentation/webkit/wkwebview ; https://developer.apple.com/documentation/webkit/wkwebsitedatastore ; https://developer.apple.com/documentation/webkit/wknavigationdelegate ; https://developer.apple.com/documentation/webkit/wkuidelegate ; https://docs.rs/objc2-web-kit/0.3.2/objc2_web_kit/ ; exact project lockfiles, Task 00010 production tests, and `proofs/macos-wkwebview-production-boundary/macos-wkwebview-production-boundary-evidence-2026-07-22.md`.

## RBL-017 — Task 00011 signed extension package lock and ownership boundary

- **Access date:** 2026-07-22.
- **Decision investigated:** Which exact local Rust dependencies and ownership split satisfy signed immutable Plugin/Skill packages, compatibility, bounded discovery, executable-hook review, and transactional publication without transferring authority to the renderer or package peer.
- **Exact versions observed:** `ed25519-dalek 3.0.0` with default features disabled, `semver 1.0.28`, `walkdir 2.5.0`, and the existing `sha2 0.11.0`, resolved in the verified local Cargo graph.
- **Selected conclusion:** Rust owns strict schemas, dual Ed25519 origin/content verification, SHA-256 inventory and content addressing, SemVer compatibility, bounded no-follow traversal, trust/revocation, immutable storage/selectors, lifecycle/audit, source-qualified Skill resolution, and restart reconciliation. The renderer receives only typed commands and service projections. Reviewed hooks execute only in the project-owned pinned Node sandbox and may propose effects only through the C4OS Action Gateway. Local Git catalogs resolve through the fixed `/usr/bin/git` boundary to immutable commits before package validation.
- **Rejected alternatives:** Executable renderer metadata, arbitrary package scripts or uninstall hooks, ambient package paths, symlink-following discovery, floating versions or refs, signatures treated as execution authority, peer-owned policy decisions, and package code receiving direct filesystem/process/network/credential/runtime authority.
- **Implementation impact:** Task 00011 locks the dependency graph, strict manifests and frontmatter, private immutable store, five-source Skill resolver, declarative Plugin contributions, reviewed-hook protocol, activation/revocation/update/rollback transactions, exact selector publication contract, restart migration/recovery, and real Plugins/Skills Settings surfaces.
- **Residual risk:** Signing-key operations and distribution remain outside this local-development task. Dependency, manifest, Node-runtime, Git, or macOS sandbox changes require a new compatibility row and the hostile/package/native matrix. Parent-directory sync degradation after an atomic selector rename is logged; the already committed database document remains restart authority and repairs any lost projection.
- **Verification:** Exact Cargo lock inspection; strict package/store unit tests; hostile lifecycle, persistence, and transaction suites; post-rename selector failure injection; complete Rust/renderer regression; rebuilt app bundle; native reviewed-hook sandbox run; native Plugin/Skill restart walkthrough; and independent P0=0/P1=0 acceptance.
- **Sources:** Exact local `Cargo.toml`/`Cargo.lock`, `src/backend/src/extension/`, Task 00011 tests, renderer service/Settings tests, rebuilt native bundle, and Task 00011 evidence images. No additional online source was required because the decision records the exact resolved local graph and implemented ownership boundary.

## RBL-018 — Task 00012 MCP 2025-11-25 client and transport lock

- **Access date:** 2026-07-22.
- **Decision investigated:** Which maintained Rust client and exact feature graph satisfy the Frozen MCP `2025-11-25` lifecycle for supervised STDIO and authenticated Streamable HTTP without moving C4OS policy, credential, process, persistence, or audit authority into the protocol SDK.
- **Exact versions observed:** Official `rmcp 2.2.0`, `tokio 1.53.0`, `reqwest 0.13.4`, `process-wrap 9.1.0`, and their exact local resolved graph. `rmcp` is pinned with default features disabled and only `client`, `transport-child-process`, `transport-streamable-http-client-reqwest`, and `reqwest`; the local `cargo check --lib` completed successfully in 125 seconds.
- **Selected conclusion:** `rmcp` supplies typed protocol messages and transport/session mechanics while C4OS explicitly advertises and post-validates `2025-11-25`, supervises each lifecycle generation, resolves only absolute STDIO executables with sanitized environments, disables transparent HTTP reinitialization, restricts HTTP origins/redirects/credentials, bounds pagination/time/output, treats every peer field as hostile, and routes authority-bearing work through capability preflight, Action Gateway, redaction, audit, cancellation, and revocation. Durable definitions contain only literal non-secret values or opaque vault/environment references.
- **Rejected alternatives:** The inherited ad hoc `2025-03-26` Node proof client, SDK defaults or latest-version negotiation, server/macros/OAuth features, ambient PATH or environment authority, raw bearer/header values in configuration, stateless or automatic session replacement, unrestricted redirects/DNS destinations, direct peer effects, and treating SDK conformance status as C4OS acceptance.
- **Implementation impact:** Task 00012 owns strict scoped definitions, app database state/journal migration, shared credential authority, exact initialize/capability checks, both supervised transports, tools/resources/sampling/notifications/cancellation/restart/recovery, stale-generation rejection, security mediation, hostile fixtures, production MCP Settings states, regression, and native evidence.
- **Residual risk:** `rmcp` is a Tier 2 SDK and sampling APIs are marked for a future protocol revision; neither changes the Frozen version. SDK, reqwest/rustls, macOS process, DNS, or MCP protocol changes require a new compatibility row and complete conformance/hostile/native matrix. OAuth and public remote-server discovery are not implied by bearer-token support. Task 00015 closed the terminal invocation persistence finding by closing the parked worker and publishing explicit recovery state. A narrower cancellation window remains after the final cancellation check but before Ready commit; the registry remains quiesced, so no subsequent broker effect is authorized, while teardown may await disable/restart.
- **Verification:** Exact Cargo lock and feature inspection; strict protocol export; STDIO and authenticated Streamable HTTP fixtures; lifecycle, persistence, broker, sampling, credential, Action Gateway, crash/restart, cancellation, hostile-content, renderer, native bundle, secret-absence, and final process-cleanup checks; complete Rust/renderer/Playwright regressions; native Settings/Chat walkthrough; and independent acceptance with P0=0, P1=0, P2=1, and P3=0.
- **Sources:** https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle ; https://modelcontextprotocol.io/specification/2025-11-25/basic/transports ; https://modelcontextprotocol.io/specification/2025-11-25/client/sampling ; https://modelcontextprotocol.io/docs/tutorials/security/security_best_practices ; https://modelcontextprotocol.io/docs/sdk ; https://github.com/modelcontextprotocol/rust-sdk/releases/tag/rmcp-v2.2.0 ; exact local `Cargo.toml`/`Cargo.lock` and the Task 00012 dependency compile.

## RBL-019 — Task 00013 onboarding and Settings authority composition

- **Access date:** 2026-07-23.
- **Decision investigated:** Whether complete onboarding and Settings integration required a new framework or dependency, and how provider, credential, model, runtime, configuration, policy, Workspace, extension, and MCP writers could compose without transferring authority to the renderer or allowing cross-domain races.
- **Observed baseline:** The Frozen launch/settings contract already assigns provider readiness, opaque credential custody, capability truth, runtime defaults, policy evaluation, configuration precedence, Workspace activation, and extension/MCP lifecycle to Rust-owned services. The locked React/Tauri graph already provides the single Settings router, accessible controls, native menu, and validated acceptance-home seam. No new dependency or external product behavior was required.
- **Selected conclusion:** Compose the existing Rust services behind typed snapshot/generation commands. Serialize provider Save & Test across credential and connectivity approvals; freeze all related model/provider writers while approval is pending; hold policy/configuration coordination through durable commit and compensate every later Workspace activation failure; quarantine host and exact-generation routes after failed provider reload; and keep QA-only diagnostics off production routes. The renderer owns draft/edit projection and focus only.
- **Rejected alternatives:** Hardcoded onboarding records, renderer-simulated provider success, renderer-held secrets, independent overlapping provider/model writers, policy reconciliation after runtime publication, best-effort reload without quarantine, effective policy results reused as editable category choices, QA route composition in production, and source-only acceptance without rebuilt native restart evidence.
- **Implementation impact:** Task 00013 introduces no dependency change. It completes production onboarding, provider/model/runtime/configuration/policy Settings, policy-aware Workspace activation, full composer serialization before Terminal/Browser mutation, native menu/Back/restart behavior, deterministic provider degradation/recovery, and exact secret/process/log verification.
- **Residual risk:** A crash after credential creation but before durable profile publication can leave an encrypted orphan credential. Task 00015A classified it as secret-safe and authority-free because it contains no plaintext and no published provider route can reference it. It remains a bounded cleanup P2, not an authorization or disclosure path.
- **Verification:** Complete renderer 69-file/361-test and 43/43 Playwright matrices; serialized Rust workspace regression; protocol export; rebuilt bundle and three packaged-tree checks; ignored MCP/OpenCode/runtime tiers; private-TLS native golden; production native onboarding, every Settings destination, degraded/recovery, responsive, restart, secret/log/process, and cleanup matrix; three independent reviews with P0=0 and P1=0.
- **Sources:** Exact local lockfiles and production source/tests; `.agents/references/00007-launch-and-settings-contract.md`; `.agents/resources/native/task-00013-acceptance.md`. No new online source was required because the decision composes already frozen local authority and version contracts.

## RBL-020 — Task 00014 local update, sticky recovery, and redacted diagnostics authority

- **Access date:** 2026-07-24.
- **Decision investigated:** Whether local-development application, runtime, and Plugin updates; startup recovery; configuration-policy compensation; and operational diagnostics required a new updater, logging dependency, or renderer authority, and how macOS path aliases could remain compatible without weakening final-component symlink denial.
- **Observed baseline:** The Frozen implementation and launch/Settings contracts already require independent update channels, last-known-good recovery, explicit external signing/distribution gates, native-owned startup authority, redacted diagnostics, and honest degraded states. The locked Rust/Tauri/React graph already provides durable SQLite state, content hashing, native process supervision, typed protocol export, and one production Settings composition. No new dependency or external product behavior was required.
- **Selected conclusion:** Keep local update discovery and verified staging inside Rust, with separate application/runtime/Plugin channel generations, transactional journals, revocation, rollback, cleanup, and recovery actions. A renderer Activate action exists only for a staged non-revoked candidate whose native recovery action is absent or exactly `activate_staged`; rebuild-required candidates expose only truthful rebuild/revoke guidance. App and Workspace configuration errors carry a sticky recovery bit that ordinary watcher/content/no-op/self-write events cannot clear; only proven compensation or a successful observer reconciliation of current durable last-known-good authority may clear it. Stable rejected content settles after watcher coverage, and exact configuration diagnostics are deduplicated and capped at 128. On macOS, canonicalize only the existing SQLite parent and append the unchanged final filename for the `NOFOLLOW` open; inspect a held backup through an immutable descriptor source.
- **Rejected alternatives:** A public or signed updater claim, one shared app/runtime/Plugin version switch, renderer-held candidate paths or digests, generic Activate for rebuild-required candidates, retrying stable rejected configuration forever, unbounded or duplicate diagnostics, clearing recovery on unchanged/self-write callbacks, reopening a backup path after validation, rejecting the lexical `/var` alias, following final-component symlinks, and exposing raw filesystem paths, secrets, prompts, environment values, tool payloads, or provenance in diagnostics.
- **Implementation impact:** Task 00014 introduces no dependency change. It adds production update and startup-recovery services, durable schema/state integration across runtime/extension/MCP/browser/terminal/configuration authorities, typed renderer update/diagnostic/recovery adapters, honest Configuration Settings and startup gates, fault-injection/security tests, exact protocol bindings, rebuilt-native evidence, and reciprocal UX-010/UX-013 coverage closure.
- **Residual risk:** `RestoreValidatedBackup` is not normally reachable because backup authority is captured only after successful Database startup; any repair must preserve exact-Database scope and independently persisted, revalidated authority. Pending native update operations require manual Refresh because the renderer has no bounded polling/subscription. A successful Settings policy save can leave a conservative sticky recovery projection visible until observer reconciliation, restart, or another externally activated change. Signing, notarization, feed publication, distribution, and human acceptance remain external/later gates.
- **Verification:** Complete renderer 77-file/441-test and 43/43 Playwright matrices; serialized Rust workspace regression plus final focused app/Workspace/configuration recovery tests; workspace check; exact protocol export; final debug app build and binary hash; three packaged-tree verifiers; exact-final MCP 2/2, OpenCode 7/7, stream 3/3, packaged runtime 4/4, and private-TLS 1/1 tiers; production-native staged/rebuild/revoke, redacted export, 624 px, restart, blocked startup retry, persistence/log/secret/process, original-home restoration, and cleanup matrix; three independent reviews with P0=0 and P1=0.
- **Sources:** Exact local lockfiles, production source/tests, `.agents/references/00007-launch-and-settings-contract.md`, and `.agents/resources/native/task-00014-acceptance.md`. No new online source was required because the decision implements already Frozen local authority and external-gate boundaries.

## RBL-021 — Task 00015 deterministic QA and bounded diagnostic restart closure

- **Access date:** 2026-07-24.
- **Decision investigated:** How to provide deterministic integrated QA without shipping fixture authority, how to tier the complete local macOS acceptance matrix, and why a saturated 250-record diagnostic journal degraded only on a later native restart.
- **Observed baseline:** The production and QA builds share React product surfaces, but QA requires fixed IDs, a clock, replay/reset/isolation, all 16 direct routes, and predictable native projections. Production cannot contain a fixture route, selector, marker, source map, or fallback transport. The native restart failure occurred only when the diagnostic document already retained 250 rows: the next bounded document still contained 250 rows, but atomic persistence needed one evicted prior ID plus the replacement set, exceeding the database API's incorrect 250-ID replacement limit.
- **Selected conclusion:** Select QA composition only through the build-gated entry, require an explicit `deterministic-e2e` in-page marker before the QA native transport can answer, and make the production bundle checker reject fixture markers and frontend maps. Keep production adapter behavior deterministic where the accepted route needs platform truth. For diagnostics, bound inserted and retained rows at 250 while permitting the atomic replacement scope to contain the old-plus-new union at a strict maximum of 500 IDs. Continue validating/deduplicating IDs and perform runtime-state compare-and-swap, deletes, inserts, retention, pruning, and generation publication in one immediate transaction.
- **Rejected alternatives:** Runtime URL/query activation of fixtures, production fallback to QA transport, shared mutable fixture identity, production source maps, accepting source-only QA, increasing the retained journal without a contract change, deleting the evicted row in a separate transaction, weakening diagnostic validation, relabelling persistence failure as MCP failure, or treating a healthy first launch as restart acceptance.
- **Implementation impact:** Task 00015 adds the versioned QA schema, route launcher, product-adapter state, build-specific router/store/native transport entrypoints, bundle-boundary checker, tiered scripts, complete deterministic scenarios, and final native evidence. The saturation repair changes only the database transition bound and adds maximal replacement plus restart regressions. No dependency or accepted product-scope change was required.
- **Residual risk:** A hostile cast can mutate the QA adapter's live replay array, and the fixed QA identity pill may occlude QA-only top-right chrome at narrow widths. Security retains bounded journal capacity, generic payload validation, Terminal metadata, encrypted orphan cleanup, backup reachability, post-effect lease, and narrow MCP cancellation P2s. Apple/WebKit and Computer Use can emit sandbox/AppKit geometry notices during accessibility capture. None grants production QA authority or weakens the product Action Gateway.
- **Verification:** Renderer formatting/lint/typecheck, 79-file/454-test unit matrix, production and QA builds, source-map/fixture bundle checks, 45/45 Playwright, serialized Rust workspace and 104-test security matrices, exact protocol, final native bundle hash, three packaged-tree verifiers, MCP HTTP 2/2, OpenCode 7/7, streaming 3/3, packaged runtime 4/4, private-TLS 1/1, all 16 native QA destinations, Light/Dark, keyboard/narrow, repeated saturated-home healthy launch with all nine startup boundaries, blocked Database recovery Retry, secret/log/process cleanup, 50/50 coverage closeout, Agent Workspace validation, and independent P0=0/P1=0 acceptance.
- **Sources:** Exact local source, lockfiles, tests, `.agents/references/00007-launch-and-settings-contract.md`, and `.agents/resources/native/task-00015-acceptance.md`. No online source was required because the decision closes the Frozen local QA, database, and acceptance boundaries without changing dependency or product truth.

## RBL-022 — OpenCode terminal-stop and restart-session convergence

- **Access date:** 2026-07-28.
- **Decision investigated:** Why an authenticated OpenRouter response with provider finish reason `stop` produced 128 repeated OpenCode model steps, and why a durable C4OS Retry failed after the application/runtime process restarted.
- **Observed upstream contract:** Exact OpenCode tag `v1.18.3` at commit `127bdb30784d508cc556c71a0f32b508a3061517` calculates a terminal `finished` condition in `packages/opencode/src/session/prompt.ts`, but its ordinary text-response path did not return `break`; the processor therefore continued the same user turn. Upstream issue `#11153` independently reports the same repeated-response behavior. OpenCode session correlation and the C4OS broker context are process-local, so a new native process cannot dispatch an existing durable Chat until the exact native session is recreated.
- **Selected conclusion:** Extend the already required C4OS downstream server-auth patch with one bounded terminal rule: after content-filter and structured-output special handling, return `break` when `finished` is true. The existing `finished` predicate deliberately excludes `tool-calls` and `unknown`, preserving tool continuation. Build flavor `c4os-auth-fd-terminal-stop.2` remains pinned to exact upstream source, Node `26.3.0`, Bun `1.3.14`, and the existing models snapshot. At the C4OS dispatch boundary, call the peer's idempotent `create_session` before every existing-chat Retry or follow-up dispatch; peer-specific session, process, correlation, and route validation remains authoritative.
- **Rejected alternatives:** Treating the provider/model output as the loop source; hiding repeated responses only in the renderer; imposing an arbitrary C4OS step cap while leaving ordinary terminal responses non-terminal; moving the real API key into OpenCode configuration; reusing stale native correlation after restart; or creating a new durable C4OS Chat to mask failed Retry continuity.
- **Implementation impact:** Patch SHA-256 is `512a8a82a157b5e7952c9637b6d4a7a03535418b868f8d25b9a06540c6f6a3b1`; manifest SHA-256 is `7fd2a8ad6dee662e18a0a06a6e3b9f6202a03ed557d4aa83ba58d800bd8a1723`; arm64 OpenCode binary SHA-256 is `99d5d922f715ea0df9605f72849c68c0404d48b43ff319cef4cf943a0ee650e8` at 137,518,946 bytes; complete native dependency-tree SHA-256 is `b743779f98c84d624462c6c789b4a8188b8b68355c7ee7dba32085027374c4ea`. Runtime pins and packaged-resource verification now require those exact values.
- **Residual risk:** This is a C4OS-owned downstream build, not an official npm OpenCode artifact. Any upstream OpenCode update must re-evaluate whether the terminal fix landed or changed shape, rebuild twice from the new exact source/toolchain, and rerun authentication, terminal-response, tool-call continuation, package integrity, fresh Chat, restart Retry, and cleanup evidence. Interactive secure-storage recovery still intentionally restarts the app so immutable credential services reopen under installation-key protection.
- **Verification:** Two independent exact-toolchain builds produced the same native bytes. Node sidecar tests passed 11/11; exact asset and bundle validators passed; the final `runtime_dispatch` target passed 26/26, including a fresh-registry retry with initially empty process-local session state; renderer passed 80 files / 476 tests; Playwright passed 47/47; protocol export matched; the full pre-final Rust library passed 297 tests / 4 intentionally ignored and the exact MCP STDIO target passed 2/2 outside the outer sandbox. In production, both a fresh Chat and a durable post-restart Retry completed in three seconds with exactly one user message, one assistant message, one `step-start`, and one `step-finish: stop`.
- **Sources:** https://github.com/anomalyco/opencode/blob/v1.18.3/packages/opencode/src/session/prompt.ts ; https://github.com/anomalyco/opencode/issues/11153 ; exact project patch/manifest, packaged binary, OpenCode SQLite records, C4OS durable session records, and Task 00022 live native walkthrough.
