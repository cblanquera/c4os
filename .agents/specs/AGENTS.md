# Agent Specs Guide

Specs are compact, AI-readable records. Keep durable facts in grouped record
files and keep generated documents disposable.

Before retiring a planning or progress source, update
`c4os-mvp/generated/source-retirement-review.md` with an explicit decision for
that source.

