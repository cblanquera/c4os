{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal foundation capability set for the main C4OS window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
